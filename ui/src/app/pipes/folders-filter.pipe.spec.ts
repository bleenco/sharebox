import { FoldersFilterPipe } from './folders-filter.pipe';

describe('FoldersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FoldersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
